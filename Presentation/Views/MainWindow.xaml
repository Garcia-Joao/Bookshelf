<Window x:Class="Bookshelf.Presentation.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Bookshelf.Presentation.Views"
        xmlns:vm="clr-namespace:Bookshelf.Presentation.ViewModels"
        xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid>
        <!-- Main Content Area -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Content Controller -->
            <telerik:RadTransitionControl x:Name="ContentController" Grid.Row="0">
                <!-- Initial content or placeholder -->
                <TextBlock Text="Main Content Area" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="24"/>
            </telerik:RadTransitionControl>

            <!-- Sidebar (Overlay) -->
            <Border x:Name="Sidebar" Background="LightGray" Width="250" VerticalAlignment="Stretch" HorizontalAlignment="Left" Visibility="Collapsed">
                <StackPanel>
                    <telerik:RadButton Content="Home"/>
                    <telerik:RadButton Content="Settings"/>
                    <telerik:RadButton Content="Help"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Toggle Button for Sidebar -->
        <telerik:RadToggleButton Content="Toggle Sidebar" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10"/>
    </Grid>

</Window>
